HILTON DISPATCH — open this on a yard computer

1. Run node server.js (or ./serve.sh) and go to http://127.0.0.1:8756
   Railway: hilton-dispatch-production.up.railway.app

2. Crew PIN is 1956. Settings admin password is 4357.

3. Four material books — do not mix:
   Store = Aug 26, 2026 yard sheet
   Flagstone = 2026 sheet, mostly $/lb
   Boulders / colored rock = boulder flyer
   Willow Creek = pit tons only. Selecting Willow Creek as origin opens this tab.

4. Billing is always 1 load. Extra site minutes and extra wait minutes are 0.
   Forklift / extra equipment $ defaults to 0.
   Put a driver or PO in Notes if the crew needs it.
   Fuel surcharge % is typed in Settings (default 0). It is that percent of the delivery fee.
   Diesel this week and week-of print as the EIA index. Pull EIA week does not change the percent.
   Dump 8% road buffer is Settings-only. Crew does not see it on the ticket.

5. Settings (4357) — confirm:
   Dump $160/hr · Small $100/hr · Forklift $160/hr
   Dump-road buffer 8%
   Round trip
   Nick / dispatch email = dispatch@hiltonlandscaping.com
   Paste Google Maps API key (Maps JS + Directions + Geocoding + Places API New)
   Or set GOOGLE_MAPS_API_KEY on Railway. Saving rates asks for a second confirm.

6. Print invoice + route sheet. Email Nick / dispatch still goes to dispatch@hiltonlandscaping.com.

Tickets live on the Railway server so every yard sees the same board after refresh.
